function hello1(neam) {
    return `Hello, ${name}`;
}

const hello2 = function(name) {
    return `お早う, ${name}`;
}

const hello3 = (name) => {return `안녕하세요, ${name}`}

const hello5 = name => `다시한번 안녕하세요 ${name}`;

const helloEvryone = (name='여러분') => `안녕하세요 ${name}`